import styled from "styled-components";
import { Colors } from "../../../../../constants/colors";

export const ShowMoreCommentsText = styled.p`
  margin: 10px 0 15px 0;
  color: ${Colors["gray-primary"]};
  cursor: pointer;
`;
